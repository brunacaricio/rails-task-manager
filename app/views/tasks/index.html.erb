<div class="container mt-3">
  <h2>My tasks</h2>

  <% @tasks.each do |task| %>
    <li mt-2><%= link_to task.title, task_path(task[:id]) %>
  <% end %>

  <p class="mt-4"><%= link_to 'Add a new task', tasks_new_path %></p>
</div>
